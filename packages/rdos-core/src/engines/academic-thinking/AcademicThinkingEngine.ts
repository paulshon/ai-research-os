import { BaseEngine } from "../BaseEngine";

/** Academic Thinking Engine (L3) */
export class AcademicThinkingEngine extends BaseEngine {
  readonly key = "academic-thinking";
  readonly title = "Academic Thinking Engine";
  readonly modules = ["Argumentation", "CriticalReading", "EvidenceWeighing"]; // L4 Module Layer

}
