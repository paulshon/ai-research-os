/**
 * Editor Core — Tiptap + Yjs collaborative editor engine
 * Shared between web and desktop apps
 */

export { default as EditorConfig } from "./editor-config";

export const EDITOR_DEFAULTS = {
  fontSize: 14,
  fontFamily: "'Noto Serif KR', serif",
  lineHeight: 1.8,
  maxWidth: 720,
};
