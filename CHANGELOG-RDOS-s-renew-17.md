# CHANGELOG — RDOS s-renew-17

## 1. 어시스턴트 — Gemini 직결 (내부 엔진 경유 제거)
- `app/api/assistant/route.ts` 전면 재작성, `lib/assistant/retriever.ts` **삭제**.
- 이전 경로 : 질문 → RDOS 내부 지식 검색(RAG) → FastAPI 프록시 → Gemini
- 현재 경로 : 질문 → **Gemini 2.5 Flash 직접 호출** → 답변
- 모든 학술 질의응답이 AI-Research-OS 내부 엔진을 **거치지 않는다**.
  경유지가 사라져 지연·실패 지점도 함께 사라졌다.
- 키 : ① 설정에 저장한 사용자 키 → ② 서버 `GEMINI_API_KEY`.
  실패 시 Gemini 가 준 실제 오류 메시지(`error.message`, `finishReason`, `blockReason`)를 그대로 표시한다.

## 2. 오브 — 확대 · 윤곽 선명화
| 항목 | 이전 | 이후 |
|---|---|---|
| 크기(데스크탑) | 140px | **176px** |
| 크기(모바일) | 104px | **124px** |
| 엣지 마스크 | 86% | **94%** (덜 흐려짐) |
| 외곽 | 없음 | **1px 링 + 안쪽 그림자** |
| 별자리 선 | α .52 / 1px | **α .78 / 1.4px** |
| 별 블록 | 1~2px | **1~3px** (앞쪽 별 확대) |
| 궤도 링 | α .28 | **α .42** |
| 중심 코어 | α .50 | **α .62** |

## 3. 데스크탑 — 본문 좌측 정렬
- `mx-auto` 를 걷어내 사이드바 바로 옆(스크린샷의 점선 위치)부터 본문이 시작된다.
  이전에는 중앙 정렬이라 좌측에 빈 띠가 생기고 내용이 오른쪽으로 밀려 보였다.
- 연구자 플랜(`max-w-[1180px]`) · RDOS(`max-w-[1080px]`) 양쪽 동일 적용.
- **오브도 함께 이동** : 화면 오른쪽 끝이 아니라 본문 컬럼의 오른쪽 끝에 붙는다.
  셸이 사이드바 폭·본문 최대폭을 위젯에 넘겨주고, 위젯이
  `right: max(24px, 100vw - 사이드바 - 본문폭 + 24px)` 로 계산한다.

  | 화면 | 본문 | 오브 right |
  |---|---|---|
  | 1440px | 268~1448px | 24px |
  | 1920px | 268~1448px | 496px |
  | 2560px | 268~1448px | 1136px |

## 4. 연구방법 — 앞맞춤 · 카드 높이 균형
- 카탈로그가 자체적으로 `max-w-5xl mx-auto` 를 갖고 있어 위쪽 히어로보다 좁고 중앙으로 밀려 있었다.
  이를 제거하고 셸과 같은 좌우 패딩(`px-4 sm:px-5 md:px-6`)을 써서 **히어로와 좌측을 앞맞춤**했다.
- 두 엔진 카드(10 STEP / 6 STEP)의 높이가 달랐다.
  그리드에 `items-stretch`, 카드를 세로 플렉스로 만들고 워크플로우 영역이 남는 높이를 흡수(`flex-1`),
  「열기」 버튼을 `mt-auto` 로 바닥에 고정해 **두 카드 바닥이 정확히 맞는다**.

## 5. 모바일
- **드로어 폭 축소** : `min(78vw,264px)` → `min(42vw,180px)` (최소 164px)

  | 화면 | 이전 | 이후 |
  |---|---|---|
  | 360px | 264px (73%) | 164px (46%) |
  | 390px | 264px (68%) | 164px (42%) |
  | 430px | 264px (61%) | 180px (42%) |

  사이드바 `aside` 가 `w-[268px]` 로 고정돼 있어 드로어 폭을 줄여도 안쪽이 넘쳤다.
  `max-w-full` 을 더해 부모 폭을 따르게 고쳤다.
- **컴팩트 모드** (`.imm-drawer`, globals.css) : 좁아진 폭에서 메뉴 이름이 잘리지 않도록
  아이콘 타일 26px, 글자 12px, 세로 패딩 축소.
- **본문 타이포 상향** : 히어로 제목 21→23px, 리드 13.5→14.5px, 수치 17→18px.
  sm(640px) 이상은 그대로라 데스크탑 인상은 바뀌지 않는다.

## 6. 검증
- `tsc --noEmit` 오류 0 · `next build` 성공(38.6s)
- `next start` : `/` 200 · `/favicon.ico` 200 · `/manifest.webmanifest` 200
- 어시스턴트 라우트에 내부 엔진 참조(`retriever` / `API_URL` / `callProxy`) **0건** 확인
