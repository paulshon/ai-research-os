# CHANGELOG — RDOS s-renew-15

renew-14 대비 변경점. 첨부 스크린샷 3장의 주석을 1:1로 반영했다.

## 1. 연구방법 — EXPERT 버튼 삭제 (스크린샷 1)
- `lib/immersive/page-meta.ts` : `/method` 의 `expert` 링크 제거.
- 기본통계 엔진은 히어로 아래 **연구방법 카탈로그 카드**에서 바로 진입한다.
  진입 경로가 카드 한 곳으로 모여 중복이 사라진다.
- 히어로(제목·수치)는 그대로 유지된다.

## 2. 문헌연구 엔진 — 빈 공백 제거 · AI 분석 위치 이동 (스크린샷 2)
- `app/(dashboard)/literature-review/page.tsx`
- **원인** : AI 분석 패널이 본문 흐름 안에서 한 줄(`flex justify-end`)을 통째로 차지해,
  탭 바와 검색 영역 사이에 패널 높이(약 120px)만큼 가로로 긴 빈 띠가 생겼다.
- **수정** :
  - 데스크탑(`xl` 1280px+)에서는 패널을 본문 위 오른쪽에 **겹쳐 띄운다**(absolute).
    흐름에서 빠지므로 빈 띠가 사라지고 검색 영역이 탭 바 바로 아래로 올라온다.
  - 위치를 오른쪽 끝에서 안쪽으로 들였다(`xl:right-24` → `2xl:right-36`).
  - 본문 컨테이너에 `xl:pr-[236px] 2xl:pr-[268px]` 로 우측 여백을 확보해 글이 패널 밑으로 들어가지 않게 했다.
  - 좁은 화면(`<xl`)에서는 겹치면 본문을 가리므로 **기존처럼 흐름 안에** 둔다.
  - 겹침 구간에서는 래퍼가 `pointer-events-none`, 패널만 `pointer-events-auto` 라
    패널 주변 빈 공간이 본문 클릭을 가로채지 않는다.

## 3. 연구 어시스턴트 — 오브 확대 · Gemini 2.5 실연동 (스크린샷 3)
- `components/assistant/research-assistant.tsx`
  - 대기 오브 **82px → 116px** (모바일 64px → 88px). 캔버스 기준 크기도 함께 조정.
  - 설정 화면이 저장하는 값을 **그대로** 읽는다:
    `gemini-api-key` → 없으면 `ai-api-key`, 모델은 `gemini-model`.
    저장된 모델이 `gemini*` 계열이면 그 모델로, 아니면 기본 `gemini-2.5-flash`.
- `app/api/assistant/route.ts`
  - 키 해석 순서를 **① 설정에 저장된 사용자 키(`x-gemini-key`) → ② 서버 `GEMINI_API_KEY`** 로 폴백하게 했다.
    이 SaaS 는 설정에서 Gemini 연결이 이미 구성되어 있으므로 둘 중 하나만 있어도 동작한다.
  - 요청이 보낸 모델이 Gemini 계열이면 그대로 사용하고, 아니면 `gemini-2.5-flash` 로 고정한다.
  - 응답에 실제 사용 모델을 함께 돌려준다.

## 4. 검증
- `tsc --noEmit` : **오류 0**
- `next build` : **성공** (30.6s, 전 라우트 컴파일)
- `next start` 스모크 : `/` 200 · `/favicon.ico` 200 · `/manifest.webmanifest` 200
- `/api/assistant` 미인증 요청 → 로그인 리다이렉트(307). 인증 게이트 정상 동작.
- 문헌연구 레이아웃 브레이크포인트 검산 (사이드바 262px 기준)

  | 뷰포트 | 본문폭 | 패널 배치 | 충돌 |
  |---|---|---|---|
  | 768px | 768px | 흐름 안 | 없음 |
  | 1024px | 762px | 흐름 안 | 없음 |
  | 1280px | 1018px | 겹침 + 우측 236px 확보 | 없음 |
  | 1920px | 1658px | 겹침 + 우측 268px 확보 | 없음 |

- 모바일 오브(88px) 좌측 여백 : 320px 화면 218px / 414px 화면 312px — 본문 가림 없음.
  하단 탭바(62px) + 세이프에어리어 위로 74px 이상 띄워 겹치지 않는다.
