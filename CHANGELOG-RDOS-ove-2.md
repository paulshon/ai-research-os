# RDOS `ove-2` — 오브 재배치·확대, 음성 인식 실패 경로 제거, 모바일 대화창 전용 디자인

적용 범위: **연구자 플랜(`app/(dashboard)`)** + **데스크탑 셸(`apps/desktop/electron`)**.
연구준비자(RDOS)는 이번에도 손대지 않았다.

---

## 1. 오브를 힌트 문구 바로 아래(점선 자리)로

기존에는 오브가 화면 우하단 근처에 떠 있어 전체메뉴와 따로 놀았다.
이제 **페이지가 오브 자리를 지정**하고 오브가 그 자리를 따라간다.

- 대기 화면이 힌트 문구 바로 아래에 `[data-orb-slot]` 사각형을 둔다
- 오브는 그 사각형의 **중심을 실측**해 정확히 그 위에 앉는다
  (`getBoundingClientRect` + `ResizeObserver` + resize·scroll·사이드바 폭 변경 추적)
- 사이드바 폭을 바꾸거나 창 크기를 바꿔도 **전체메뉴 그리드와 같은 중심선**을 유지한다

### 함께 고친 구조적 버그
루트 요소에 `transform: translateX(-50%)` 가 걸려 있었다.
**transform 이 걸린 조상은 `position:fixed` 자식의 좌표 기준이 된다** — 그래서
대화창(플로팅·바텀시트)이 화면이 아니라 오브 기준으로 잡혀 어긋날 수 있었다.
중심 정렬을 안쪽 `.ra-orbanchor` 로 옮기고 **루트에서 transform 을 걷어냈다.**

## 2. 오브를 더 크고 또렷하게

| 항목 | 이전 | 이후 |
|---|---|---|
| 대기 화면 크기 | 168px | **200px** (모바일 136px) |
| 도킹 크기 | 104px | **112px** |
| 별 개수 / 반경 | 13 / 0.275R | 15 / 0.30R |
| 선·점 밝기 | 0.42 / 0.78 / 0.24 | 0.62 / 0.95 / 0.42 |
| 외곽 | 얇은 테두리 | 내부 광원 + 2단 `drop-shadow` + 밝은 테두리 |

- 오브 아래에 **“눌러서 말하기 · Space”** 캡션을 붙여 누를 수 있는 물건임을 분명히 했다
- 청취 중에는 오브 둘레에 **붉은 파형 링**이 입력 레벨(`--lvl`)에 따라 퍼진다
- 대기 화면에서 **스페이스바 푸시투토크** — 누르는 동안 듣고 떼면 보낸다

## 3. “여전히 음성 인식이 안 된다” — 막다른 길을 없앴다

원인은 **데스크탑 앱(Electron)** 이었다.

1. **Electron 에 마이크 권한 핸들러가 없었다.**
   Electron 은 `setPermissionRequestHandler` 를 등록하지 않으면 렌더러의 `getUserMedia` 를
   승인할 방법이 없어 조용히 `NotAllowedError` 로 떨어진다. 브라우저처럼 “허용” 창도 뜨지 않는다.
   → `main.ts` 에 **`setPermissionRequestHandler` · `setPermissionCheckHandler` ·
   `setDevicePermissionHandler`** 를 등록하고(오디오만 허용, 카메라·화면 캡처는 거부),
   macOS 는 시작 시 `askForMediaAccess("microphone")` 을 호출한다.
2. **Electron 의 `webkitSpeechRecognition` 은 구글 음성 서비스 키가 없어 항상 `network` 오류다.**
   → Electron 을 감지하면 **처음부터 브라우저 STT 를 건너뛰고 서버 전사로 직행**한다(헛도는 4초 제거).
3. **서버 전사가 로그인·키에서 막혔다.**
   → `/api/stt` 는 Clerk 미설정 환경에서 **인증을 요구하지 않고**,
     키가 없으면 `code: "no-key"` 와 함께 무엇을 해야 하는지 알려준다.
4. **키를 넣을 곳이 멀었다.**
   → 대화창 **⚙ 설정 안에서 Gemini 키를 바로 입력·저장**할 수 있게 했다.
5. **정말 되는지 확인할 방법이 없었다.**
   → **“전사 테스트(3초)”** 버튼 — 3초 녹음 → `/api/stt` → **받아쓴 문장을 그대로 표시**한다.
     **“진단 복사”** 로 `platform·desktopApp·secure·browserSTT·geminiKey·origin` 한 줄을 복사할 수 있다.
6. **누르기 전에 미리 알려준다.**
   서버 전사 경로인데 키가 없으면 대화창 빈 화면에 **호박색 배너**가 떠서 한 번에 설정으로 보낸다.

### 인식률 관련 추가 보정
- 기본 감도 `0.018 → 0.012` (조용한 노트북 마이크 대응)
- 권한 창을 늦게 눌러도 불리하지 않도록 **녹음이 실제 시작될 때 타이머를 다시 잡는다**
- 무음 판정 완화 — 약한 소리라도 들어오면 계속 듣고, **완전 무음 12초**일 때만 종료
- 0.25초 이상 녹음됐다면 약한 소리라도 **무조건 서버로 보낸다**
- 마이크 버튼을 더 이상 **비활성화하지 않는다** — 눌러서 정확한 실패 사유를 보게 한다

## 4. 모바일 대화창 전용 디자인

“너무 크다”는 지적을 반영해 **화면 절반 아래**를 기본으로 잡았다.

- 3단 스냅: **`min(34dvh,280px)` / `min(52dvh,430px)`(기본) / `92dvh`**
  손잡이를 위로 끌면 커지고, 아래로 끌면 작아지고, **크게 끌어내리면 닫힌다**. 두 번 누르면 최대↔기본.
- 자유 이동·자유 리사이즈는 끄고(손가락에 맞지 않는다) **글자 크기 조절은 유지**
- 터치 타깃 **44px**, 입력창 16px(iOS 자동 확대 방지), pill 형태
- **가상 키보드 대응** — `visualViewport` 로 가려진 높이만큼 시트를 들어올린다
- 세이프 에어리어(홈 인디케이터)·하단 탭바 여백 반영
- 데스크탑 전용 버튼(창 크기 초기화·고정)은 모바일에서 숨김
- 360px 미만·가로 화면 전용 보정 규칙 추가

---

## 검증

```
tsc --noEmit (web)      오류 0
tsc --noEmit (desktop)  오류 0
next build              ✓ Compiled successfully
런타임 점검
  /dashboard 200   ra-root ra-center · data-orb-slot · ra-orbanchor · "눌러서 말하기 · Space"
  /research  200   ra-root ra-dock
  CONSTELLATION 0 · SCHOLAR-DOS 0 · 대시보드 잔재 0
  /api/stt   키 없이 호출 시 {"code":"no-key"} + 안내 문구 (이전에는 로그인 401 로 막혔음)
```

## 사용자가 할 일 (한 번만)

1. 오브 대화창 → **⚙** → **Gemini API 키** 입력 후 저장
2. **전사 테스트(3초)** 를 눌러 받아쓴 문장이 뜨는지 확인
3. 뜨지 않으면 **진단 복사** 결과를 그대로 알려주면 원인을 특정할 수 있다

> 서버에 `GEMINI_API_KEY` 를 넣어두면 사용자는 1번을 하지 않아도 된다.
