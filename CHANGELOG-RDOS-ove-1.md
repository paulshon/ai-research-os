# RDOS `ove-1` — 오브 음성 인식 정상화 · Voice-First UI v2.0 반영

> 적용 범위: **연구자 플랜(`app/(dashboard)`)에 한정**한다.
> **연구준비자(RDOS, `app/(rdos)`)는 손대지 않았다.**

---

## 1. 오브 음성 인식이 안 되던 문제 (최우선)

데스크탑·안드로이드 양쪽에서 인식이 되지 않던 원인을 모두 짚어 다시 만들었다.

| # | 원인 | 조치 |
|---|------|------|
| 1 | **보안 컨텍스트** — `http://192.168.x.x` 로 접속하면 `SpeechRecognition`·`getUserMedia` 가 조용히 실패 | 시작 전에 진단하고 원인을 한국어로 안내 |
| 2 | **권한 미프라이밍** — `webkitSpeechRecognition` 만 호출하면 안드로이드·PWA·WebView 에서 권한창이 안 뜸 | `getUserMedia` 로 **권한을 먼저 확보**한 뒤 인식을 시작 |
| 3 | **장치 충돌** — 안드로이드 크롬은 마이크 스트림이 열려 있으면 SR 이 `audio-capture` 로 죽음 | 권한 확인 직후 **스트림을 즉시 반납**하고 SR 을 켬 |
| 4 | **조기 종료** — 안드로이드는 짧은 무음에도 `onend` 발생 | 사용자가 멈추기 전까지 **자동 재시작 루프** (최대 60초, `no-speech` 3회면 종료) |
| 5 | **엔진 부재** — Electron 데스크탑·Firefox·Safari·삼성인터넷·인앱 브라우저에는 SR 이 없거나 `network` 오류 | **서버 전사 폴백** 신설: 16 kHz 모노 WAV 녹음 → `POST /api/stt` → Gemini 받아쓰기 |
| 6 | **기기마다 다른 설정** | 입력 장치·언어·엔진·감도·잡음제거를 **기기에 저장**하고 앱 전체가 같은 값을 공유 |

### 새 파일
- `lib/voice/speech.ts` — 진단 · 권한 · 브라우저 STT · 서버 전사 · 오류 매핑
- `hooks/use-voice-input.ts` — 화면 공통 훅 (`phase / level / partial / notice`)
- `app/api/stt/route.ts` — Gemini 전사 API (키 경로는 어시스턴트와 동일: `X-Gemini-Key` → `GEMINI_API_KEY`)
- `components/assistant/voice-settings.tsx` — 창 안의 **진단 → 권한 → 장치 선택 → 10초 마이크 테스트** 패널

### 사용자가 바로 확인하는 법
대화창 헤더의 **⚙ 버튼** → 4개 진단등(보안 연결 / 마이크 접근 / 브라우저 인식 / 권한)과
**[권한 요청] [마이크 테스트]** 로 어느 단계에서 막혔는지 스스로 확인할 수 있다.
브라우저 인식이 빨간불이어도 **자동으로 서버 전사로 넘어가 인식이 된다.**

---

## 2. 오브 대화창 → Floating Window

- 헤더 드래그로 **이동**. 헤더 버튼·리사이즈 영역에는 `data-no-drag` 를 붙여 **영역 충돌 없음**
- 우측 하단 코너로 **가로·세로 독립 리사이즈** (코너 `nwse` / 우측 `ew` / 하단 `ns`)
  - 도킹 480×620 (최소 360×420) · 중앙 960×680 (최소 640×480), 최대는 뷰포트 비례
- **A− / A+ 6단계**(0.85 · 0.925 · **1.00** · 1.10 · 1.25 · 1.40) — `--fw-scale` 변수 하나로
  본문·상태·버튼·입력창·칩이 함께 대응, 고정 높이 대신 `min-height`+`em` 이라 레이아웃이 깨지지 않음
- 크기·위치·배율·고정 여부를 **모드별로 `localStorage(ui.orbWindow)` 에 저장** → 다시 열어도 유지
- 드래그·리사이즈 중에는 `data-interacting` 으로 **모든 전환을 정지**해 조작을 방해하지 않음
- 헤더가 최소 80px 화면 안에 남도록 클램프, 가장자리 16px 자석(Alt 로 해제)
- 모바일(<768px)은 **바텀시트**로 전환, 글자 크기 조절은 유지

## 3. 중앙 모드 ↔ 도킹 모드 모프

오브가 **중앙(전체메뉴 아래)** 이면 대화창이 가로 확장형(본문 16px, 패딩 32, 입력창 전폭 pill 60px),
**우하단 도킹**이면 세로형으로 바뀐다. 전환은 **360ms `cubic-bezier(.22,1,.36,1)`** 로
너비·높이·라운드·패딩을 보간한다 — 순간 점프하지 않는다.

## 4. 문구·메뉴 삭제

- 대화창 헤더의 **`CONSTELLATION` / `SCHOLAR-DOS` 코드네임 배지 제거** → 상태 텍스트만
- **대시보드 삭제** — 사이드바 항목과 페이지 내용(진행률 게이지·빠른 엔진 진입·오늘의 AI 인사이트·연구 흐름 바)을 모두 제거.
  `/dashboard` 라우트는 **대기 화면(S0)** 으로 대체(기존 링크가 깨지지 않도록 경로만 유지)
- 우측 상단의 **논문일정 · 참고문헌 정리 버튼 삭제** (기능 자체는 유지 — 중앙 메뉴 "+ 더 보기"·오브 발화로 진입)

## 5. 전체메뉴를 중앙으로 · 오브는 그 아래

- `components/dashboard/menu-grid.tsx` — 8개 카드 그리드(연구 설계 · 문헌 연구 · 연구방법 · 논문 작성 ·
  논문 분석 · 논문 크리틱 · 검토·검증 · 참고문헌 정리), 30ms stagger 등장, 선택 카드 펄스 후 이동
- 그리드 아래 **힌트 2줄** → 그 아래 **오브(168px)**. 메뉴를 부르면 오브는 **우하단으로 하강·도킹**
- 카드 클릭 · 대화창 입력 · 발화가 **모두 같은 인텐트 경로**(`classifyIntent`)를 탄다.
  신뢰도 ≥0.75 즉시 이동, 0.34~0.75 후보 칩 3개 되묻기, 그 아래는 Gemini 질문으로 처리

## 6. 사이드바 — 리사이즈 + 작업 히스토리

- 우측 경계선 드래그로 **200~480px 자유 조절**(168px 미만이면 64px 접힘), 더블클릭 리셋,
  키보드 ←/→ 조절, `localStorage(ui.sidebarWidth)` 저장. 폭은 오브 중앙 정렬에 즉시 반영
- **작업 히스토리 레일** 신설 — 제목 + **연·월·일·시각(`2026-08-10 18:26`)**,
  오늘/어제/지난 7일/지난 30일/연도 그룹, 검색, 이름 변경·삭제(5초 되돌리기)
- 저장은 **IndexedDB `studiumr/sessions`**, 실패 시 localStorage 자동 강등, `BroadcastChannel` 로 탭 동기화
- **그대로 유지**: 프로젝트 워크스페이스 블록(자동저장·새 프로젝트·저장/불러오기·임시저장/초기화),
  사용자 프로필, **관리자 메뉴**, 플랜 게이지

## 7. 부수 수정

- `components/dashboard/sidebar-items.ts` — 작업 카탈로그 단일 정본 + 인텐트 분류기 분리
  (기존 `@/components/dashboard/sidebar` import 경로는 재수출로 100% 호환)
- `types/svix.d.ts` — svix 1.94.0 이 `dist/webhook.d.ts` 를 배포에서 빠뜨려
  `next build` 가 실패하던 문제를 타입 보강으로 해결(런타임 영향 없음)
- i18n: `ko/en/zh-ove.ts` 신설 (home · history · orb · voice 문구)

---

## 검증 결과

```
tsc --noEmit          오류 0
next build            ✓ Compiled successfully  (전 라우트 생성 완료)
next start 런타임 점검
  /dashboard 200      메뉴 카드 8개 · 힌트 2줄 · ra-center(중앙 오브) 렌더
  /research  200      ra-dock(우하단 도킹) 렌더
  CONSTELLATION       0건   SCHOLAR-DOS 0건   논문일정 버튼 0건
  대시보드 잔재       "오늘의 AI 인사이트" "빠른 엔진 진입" "전체 진행률" 모두 0건
  /api/stt            401 (미인증 차단 정상)
```

## 남은 확인 사항

- 서버 전사(`/api/stt`)는 **Gemini API 키**가 있어야 동작한다. 설정 > API 키 또는 서버 `GEMINI_API_KEY`.
- 배포 도메인은 반드시 **https** 여야 한다. 사내 IP(http) 접속에서는 어떤 브라우저도 마이크를 열지 않는다.
- 안드로이드는 Chrome 권장. 카카오톡·인스타그램 등 **인앱 브라우저**는 SR 이 없어 자동으로 서버 전사로 동작한다.
