/**
 * Cloud Sync Engine
 * Syncs local metadata to Supabase.
 * Files stay local (Local First), only metadata goes to cloud.
 */

export async function initSyncEngine(): Promise<void> {
  console.log("☁️ Cloud sync engine initialized");
}
