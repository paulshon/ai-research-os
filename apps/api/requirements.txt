# ═══════════════════════════════════════════
# AI Research OS — Python Dependencies
# HF Spaces 무료 (512MB RAM) 최적화
# ═══════════════════════════════════════════

# Core
fastapi==0.115.0
uvicorn[standard]==0.32.0
pydantic==2.10.0
pydantic-settings==2.7.0

# HTTP Client (Gemini API, CrossRef API 호출)
httpx==0.28.0

# File Upload
python-multipart==0.0.17

# Document Parsing
PyMuPDF==1.25.0
python-docx==1.1.2

# Auth (JWT 검증)
python-jose[cryptography]==3.3.0

# Supabase (선택 — 없으면 로컬 모드로 동작)
supabase==2.11.0

# Testing (v4 — CI 의 pytest 실행을 위해 추가)
pytest==8.3.0
